import { describe, test, expect } from 'vitest'

describe('Test Setup', () => {
  test('vitest is working', () => {
    expect(true).toBe(true)
  })

  test('math works', () => {
    expect(2 + 2).toBe(4)
  })
})
